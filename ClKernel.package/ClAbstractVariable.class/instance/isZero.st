testing
isZero
	"There is a shortcut in SmallInteger divide to generate a divide by zero error before allowing the dispatch based on the arguments to happen. To force it, we need to answer to isZero."

	^ false